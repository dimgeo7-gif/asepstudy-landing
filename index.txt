<!DOCTYPE html>
<html lang="el">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ASEP Study — Έξυπνος Προσομοιωτής Διαγωνισμού ΑΣΕΠ</title>
<meta name="description" content="Προσομοίωση τεστ ΑΣΕΠ με έξυπνο μητρώο λαθών που μαθαίνει μαζί σου. Δωρεάν έναρξη.">
<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><rect width=%22100%22 height=%22100%22 rx=%2220%22 fill=%22%2314213A%22/><text x=%2250%22 y=%2268%22 font-size=%2260%22 text-anchor=%22middle%22 fill=%22%23B23A2E%22>✓</text></svg>">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,500;9..144,600;9..144,700;9..144,900&family=Public+Sans:wght@400;500;600;700&family=IBM+Plex+Mono:wght@500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --ink: #14213A;
    --ink-2: #1C2C4C;
    --ink-3: #24365A;
    --paper: #ECE7DA;
    --paper-2: #F5F2E9;
    --stamp: #B23A2E;
    --stamp-dark: #8E2E24;
    --gold: #C9A227;
    --sage: #4F7A64;
    --mist: #9AA3B8;
    --mist-dim: #6E7890;
    --line: rgba(236, 231, 218, 0.14);
    --radius: 14px;
    --display: 'Fraunces', serif;
    --body: 'Public Sans', system-ui, sans-serif;
    --mono: 'IBM Plex Mono', monospace;
  }

  * { box-sizing: border-box; }
  html { scroll-behavior: smooth; }
  body {
    margin: 0;
    background: var(--ink);
    color: var(--paper-2);
    font-family: var(--body);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
  }
  img, svg { display: block; max-width: 100%; }
  a { color: inherit; }
  h1, h2, h3 { font-family: var(--display); margin: 0; letter-spacing: -0.01em; }
  p { margin: 0; }
  .wrap { max-width: 1160px; margin: 0 auto; padding: 0 28px; }
  .eyebrow {
    font-family: var(--mono);
    font-size: 0.72rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--gold);
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }
  .eyebrow::before {
    content: "";
    width: 20px;
    height: 1px;
    background: var(--gold);
  }
  :focus-visible {
    outline: 2px solid var(--gold);
    outline-offset: 3px;
  }
  ::selection { background: var(--stamp); color: #fff; }

  /* ---------- NAV ---------- */
  nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(20, 33, 58, 0.88);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--line);
  }
  .nav-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 28px;
    max-width: 1160px;
    margin: 0 auto;
  }
  .logo {
    display: flex;
    align-items: center;
    gap: 10px;
    font-family: var(--display);
    font-weight: 700;
    font-size: 1.15rem;
    color: var(--paper-2);
    text-decoration: none;
  }
  .logo-mark {
    width: 30px; height: 30px;
    border-radius: 8px;
    background: var(--stamp);
    display: flex; align-items: center; justify-content: center;
    color: #fff; font-family: var(--mono); font-size: 0.9rem; font-weight: 600;
    transform: rotate(-4deg);
  }
  .nav-links {
    display: flex;
    gap: 32px;
    font-size: 0.92rem;
    color: var(--mist);
  }
  .nav-links a { text-decoration: none; transition: color 0.15s; }
  .nav-links a:hover { color: var(--paper-2); }
  .nav-cta { display: flex; align-items: center; gap: 18px; }
  .nav-cta .signin { font-size: 0.9rem; color: var(--mist); text-decoration: none; }
  .nav-cta .signin:hover { color: var(--paper-2); }
  @media (max-width: 760px) { .nav-links { display: none; } }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-family: var(--body);
    font-weight: 600;
    font-size: 0.94rem;
    padding: 12px 22px;
    border-radius: 10px;
    text-decoration: none;
    border: 1px solid transparent;
    transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.15s;
    cursor: pointer;
  }
  .btn-primary {
    background: var(--stamp);
    color: #fff;
    box-shadow: 0 1px 0 rgba(0,0,0,0.2), 0 8px 20px -8px rgba(178,58,46,0.6);
  }
  .btn-primary:hover { background: var(--stamp-dark); transform: translateY(-1px); }
  .btn-ghost {
    background: transparent;
    color: var(--paper-2);
    border-color: var(--line);
  }
  .btn-ghost:hover { border-color: var(--mist); }
  .btn-lg { padding: 15px 28px; font-size: 1.02rem; }

  /* ---------- HERO ---------- */
  .hero {
    padding: 92px 0 60px;
    position: relative;
    overflow: hidden;
  }
  .hero::before {
    content: "";
    position: absolute;
    top: -200px; right: -160px;
    width: 560px; height: 560px;
    background: radial-gradient(circle, rgba(178,58,46,0.16), transparent 70%);
    pointer-events: none;
  }
  .hero-grid {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 56px;
    align-items: center;
  }
  @media (max-width: 900px) {
    .hero-grid { grid-template-columns: 1fr; }
    .hero { padding-top: 40px; }
  }
  .hero h1 {
    font-size: clamp(2.3rem, 4.6vw, 3.6rem);
    font-weight: 700;
    line-height: 1.08;
    color: #fff;
    margin: 20px 0 22px;
  }
  .hero h1 em {
    font-style: normal;
    color: var(--gold);
  }
  .hero-sub {
    font-size: 1.12rem;
    color: var(--mist);
    max-width: 46ch;
    margin-bottom: 34px;
  }
  .hero-actions { display: flex; gap: 16px; flex-wrap: wrap; margin-bottom: 30px; }
  .hero-note {
    font-size: 0.85rem;
    color: var(--mist-dim);
    display: flex; align-items: center; gap: 8px;
  }
  .hero-note svg { width: 15px; height: 15px; color: var(--sage); flex-shrink: 0; }

  /* ---------- SIGNATURE CARD ---------- */
  .file-card-stage { position: relative; display: flex; justify-content: center; }
  .file-card {
    width: 100%;
    max-width: 380px;
    background: var(--paper);
    color: var(--ink);
    border-radius: var(--radius);
    padding: 26px 26px 24px;
    box-shadow: 0 30px 60px -20px rgba(0,0,0,0.55), 0 2px 0 rgba(0,0,0,0.05) inset;
    position: relative;
    transform: rotate(-1.4deg);
  }
  .file-card::before {
    content: "";
    position: absolute;
    left: 26px; right: 26px; top: 58px;
    height: 0;
    border-top: 1px dashed rgba(20,33,58,0.18);
  }
  .file-head {
    display: flex; justify-content: space-between; align-items: flex-start;
    font-family: var(--mono); font-size: 0.68rem; letter-spacing: 0.06em;
    color: rgba(20,33,58,0.55); text-transform: uppercase;
    padding-bottom: 16px;
  }
  .file-head b { color: var(--ink); }
  .file-q {
    font-family: var(--body); font-weight: 600; font-size: 0.98rem;
    margin: 20px 0 14px; color: var(--ink);
  }
  .file-opt {
    font-size: 0.88rem; color: rgba(20,33,58,0.75);
    padding: 7px 0 7px 26px; position: relative;
  }
  .file-opt::before {
    content: ""; position: absolute; left: 0; top: 11px;
    width: 14px; height: 14px; border-radius: 50%;
    border: 1.5px solid rgba(20,33,58,0.3);
  }
  .file-opt.correct { color: var(--sage); font-weight: 600; }
  .file-opt.correct::before { border-color: var(--sage); background: var(--sage); }

  .stamp-overlay {
    position: absolute;
    top: 42%; right: -6px;
    font-family: var(--mono);
    font-weight: 700;
    font-size: 1.3rem;
    letter-spacing: 0.05em;
    color: var(--sage);
    border: 3px solid var(--sage);
    border-radius: 10px;
    padding: 6px 14px;
    transform: rotate(-11deg) scale(0);
    background: rgba(79,122,100,0.06);
    animation: stampIn 0.5s 1.1s cubic-bezier(.2,1.4,.4,1) forwards;
  }
  @keyframes stampIn {
    0% { transform: rotate(-11deg) scale(2.4); opacity: 0; }
    60% { opacity: 1; }
    100% { transform: rotate(-11deg) scale(1); opacity: 1; }
  }
  .file-foot {
    margin-top: 18px; padding-top: 14px;
    border-top: 1px solid rgba(20,33,58,0.1);
    display: flex; justify-content: space-between; align-items: center;
    font-family: var(--mono); font-size: 0.7rem; color: rgba(20,33,58,0.55);
  }
  .streak-dots { display: flex; gap: 5px; }
  .streak-dots span {
    width: 8px; height: 8px; border-radius: 50%;
    background: rgba(20,33,58,0.15);
  }
  .streak-dots span.on { background: var(--sage); }

  @media (prefers-reduced-motion: reduce) {
    .stamp-overlay { animation: none; transform: rotate(-11deg) scale(1); opacity: 1; }
  }

  /* ---------- STATS BAR ---------- */
  .stats-bar {
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    padding: 30px 0;
  }
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    text-align: center;
  }
  @media (max-width: 760px) { .stats-grid { grid-template-columns: repeat(2, 1fr); } }
  .stat-num {
    font-family: var(--mono);
    font-size: 1.9rem;
    font-weight: 600;
    color: var(--gold);
  }
  .stat-label {
    font-size: 0.8rem;
    color: var(--mist);
    margin-top: 4px;
  }

  /* ---------- SECTION SHELL ---------- */
  section.block { padding: 96px 0; }
  .section-head { max-width: 640px; margin-bottom: 56px; }
  .section-head h2 {
    font-size: clamp(1.8rem, 3vw, 2.4rem);
    color: #fff;
    margin: 16px 0 14px;
    font-weight: 600;
  }
  .section-head p { color: var(--mist); font-size: 1.04rem; }

  /* ---------- HOW IT WORKS ---------- */
  .steps {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
    border-top: 1px solid var(--line);
  }
  @media (max-width: 900px) { .steps { grid-template-columns: 1fr; } }
  .step {
    padding: 30px 26px 30px 0;
    border-right: 1px solid var(--line);
    position: relative;
  }
  .step:last-child { border-right: none; }
  @media (max-width: 900px) {
    .step { border-right: none; border-bottom: 1px solid var(--line); padding: 26px 0; }
    .step:last-child { border-bottom: none; }
  }
  .step-num {
    font-family: var(--mono);
    color: var(--stamp);
    font-size: 0.85rem;
    font-weight: 600;
  }
  .step h3 {
    font-size: 1.08rem;
    color: #fff;
    margin: 12px 0 8px;
    font-weight: 600;
    font-family: var(--body);
  }
  .step p { color: var(--mist); font-size: 0.92rem; }

  /* ---------- FEATURE GRID ---------- */
  .feature-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--line);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    overflow: hidden;
  }
  @media (max-width: 860px) { .feature-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 560px) { .feature-grid { grid-template-columns: 1fr; } }
  .feature {
    background: var(--ink-2);
    padding: 30px 26px;
  }
  .feature-icon {
    width: 38px; height: 38px;
    border-radius: 9px;
    background: var(--ink-3);
    display: flex; align-items: center; justify-content: center;
    color: var(--gold);
    margin-bottom: 18px;
  }
  .feature-icon svg { width: 19px; height: 19px; }
  .feature h3 { font-size: 1rem; color: #fff; margin-bottom: 8px; font-family: var(--body); font-weight: 600; }
  .feature p { font-size: 0.9rem; color: var(--mist); }

  /* ---------- PRICING ---------- */
  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
  }
  @media (max-width: 760px) { .pricing-grid { grid-template-columns: 1fr; } }
  .price-card {
    border: 1px solid var(--line);
    border-radius: var(--radius);
    padding: 34px 30px;
    background: var(--ink-2);
  }
  .price-card.highlight {
    background: var(--paper);
    color: var(--ink);
    border-color: var(--gold);
    position: relative;
  }
  .price-tag {
    font-family: var(--mono);
    font-size: 0.72rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gold);
  }
  .price-card.highlight .price-tag { color: var(--stamp); }
  .price-amount {
    font-family: var(--display);
    font-size: 2.4rem;
    font-weight: 700;
    margin: 14px 0 4px;
  }
  .price-card h3 { font-size: 1.15rem; margin-bottom: 4px; }
  .price-list { list-style: none; padding: 0; margin: 24px 0 28px; display: grid; gap: 12px; }
  .price-list li { display: flex; gap: 10px; font-size: 0.92rem; }
  .price-list svg { width: 16px; height: 16px; flex-shrink: 0; margin-top: 2px; color: var(--sage); }
  .price-card.highlight .price-list svg { color: var(--stamp-dark); }
  .price-card p.desc { color: var(--mist); font-size: 0.9rem; margin-top: 6px; }
  .price-card.highlight p.desc { color: rgba(20,33,58,0.65); }

  /* ---------- FAQ ---------- */
  .faq-list { display: grid; gap: 1px; background: var(--line); border: 1px solid var(--line); border-radius: var(--radius); overflow: hidden; }
  details {
    background: var(--ink-2);
    padding: 22px 26px;
  }
  summary {
    cursor: pointer;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-weight: 600;
    color: #fff;
    font-size: 1rem;
  }
  summary::-webkit-details-marker { display: none; }
  summary::after {
    content: "+";
    font-family: var(--mono);
    font-size: 1.2rem;
    color: var(--gold);
    flex-shrink: 0;
    margin-left: 20px;
    transition: transform 0.2s;
  }
  details[open] summary::after { transform: rotate(45deg); }
  details p { margin-top: 14px; color: var(--mist); font-size: 0.94rem; max-width: 62ch; }

  /* ---------- FINAL CTA ---------- */
  .cta-band {
    background: linear-gradient(135deg, var(--ink-3), var(--ink));
    border-radius: var(--radius);
    padding: 64px 48px;
    text-align: center;
    border: 1px solid var(--line);
  }
  @media (max-width: 640px) { .cta-band { padding: 48px 24px; } }
  .cta-band h2 { color: #fff; font-size: clamp(1.7rem, 3.4vw, 2.3rem); margin-bottom: 16px; font-weight: 600; }
  .cta-band p { color: var(--mist); margin-bottom: 30px; max-width: 50ch; margin-left: auto; margin-right: auto; }

  /* ---------- FOOTER ---------- */
  footer {
    border-top: 1px solid var(--line);
    padding: 44px 0 36px;
  }
  .footer-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 16px;
  }
  .footer-links { display: flex; gap: 24px; font-size: 0.86rem; color: var(--mist); }
  .footer-links a { text-decoration: none; }
  .footer-links a:hover { color: var(--paper-2); }
  .footer-copy { font-size: 0.82rem; color: var(--mist-dim); }
</style>
</head>
<body>

<nav>
  <div class="nav-inner">
    <a href="#" class="logo">
      <span class="logo-mark">✓</span>
      ASEP Study
    </a>
    <div class="nav-links">
      <a href="#features">Λειτουργίες</a>
      <a href="#how">Πώς λειτουργεί</a>
      <a href="#pricing">Δωρεάν vs Premium</a>
      <a href="#faq">Ερωτήσεις</a>
    </div>
    <div class="nav-cta">
      <a class="signin" href="https://app.asepstudy.gr">Σύνδεση</a>
      <a class="btn btn-primary" href="https://app.asepstudy.gr">Ξεκίνα Δωρεάν</a>
    </div>
  </div>
</nav>

<header class="hero">
  <div class="wrap hero-grid">
    <div>
      <span class="eyebrow">Προσομοίωση Διαγωνισμού ΑΣΕΠ</span>
      <h1>Το μητρώο λαθών σου<br>δεν ξεχνάει τίποτα —<br><em>μέχρι να το μάθεις.</em></h1>
      <p class="hero-sub">
        Κάθε λάθος που κάνεις καταγράφεται αυτόματα σε προσωπικό φάκελο.
        Το ξαναβλέπεις μέχρι να το πετύχεις 3 φορές στη σειρά — μετά βγαίνει
        από τον φάκελο. Καμία ερώτηση δεν επαναλαμβάνεται πριν καλυφθεί όλη η ύλη.
      </p>
      <div class="hero-actions">
        <a class="btn btn-primary btn-lg" href="https://app.asepstudy.gr">Ξεκίνα Δωρεάν Τώρα</a>
        <a class="btn btn-ghost btn-lg" href="#how">Πώς λειτουργεί</a>
      </div>
      <div class="hero-note">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
        2 δωρεάν τεστ, χωρίς κάρτα, χωρίς δεσμεύσεις.
      </div>
    </div>

    <div class="file-card-stage">
      <div class="file-card">
        <div class="file-head">
          <span>Φάκελος&nbsp;#0482</span>
          <span><b>Διοικητικό Δίκαιο</b></span>
        </div>
        <p class="file-q">Η μη αναγραφή στο πρακτικό της σύνθεσης του οργάνου:</p>
        <div class="file-opt">Δεν επηρεάζει τη νομιμότητα, καταρχήν</div>
        <div class="file-opt correct">Επηρεάζει το κύρος αν πάσχει ακυρότητας</div>
        <div class="file-opt">Επηρεάζει μόνο τυπικά</div>
        <div class="stamp-overlay">✓ ΜΑΘΗΜΕΝΟ</div>
        <div class="file-foot">
          <span>3/3 συνεχόμενες σωστές</span>
          <span class="streak-dots"><span class="on"></span><span class="on"></span><span class="on"></span></span>
        </div>
      </div>
    </div>
  </div>
</header>

<div class="stats-bar">
  <div class="wrap stats-grid">
    <div>
      <div class="stat-num" id="stat-questions">—</div>
      <div class="stat-label">Ερωτήσεις στη βάση</div>
    </div>
    <div>
      <div class="stat-num">2</div>
      <div class="stat-label">Δωρεάν τεστ, εξ αρχής</div>
    </div>
    <div>
      <div class="stat-num">3×</div>
      <div class="stat-label">Σωστές για να "μάθεις" μια ερώτηση</div>
    </div>
    <div>
      <div class="stat-num">0%</div>
      <div class="stat-label">Επανάληψη πριν καλυφθεί η ύλη</div>
    </div>
  </div>
</div>

<section class="block" id="how">
  <div class="wrap">
    <div class="section-head">
      <span class="eyebrow">Πώς λειτουργεί</span>
      <h2>Τέσσερα βήματα, ένας κύκλος.</h2>
      <p>Δεν είναι τυχαία ερωτηματολόγια. Είναι ένα σύστημα που θυμάται τι σε δυσκολεύει.</p>
    </div>
    <div class="steps">
      <div class="step">
        <span class="step-num">01</span>
        <h3>Κάνεις τεστ</h3>
        <p>25 ερωτήσεις, 25 λεπτά — δομή ίδια με το πραγματικό διαγωνισμό.</p>
      </div>
      <div class="step">
        <span class="step-num">02</span>
        <h3>Χτίζεται ο φάκελος</h3>
        <p>Κάθε λάθος μπαίνει αυτόματα στο προσωπικό σου μητρώο, με μετρητή.</p>
      </div>
      <div class="step">
        <span class="step-num">03</span>
        <h3>Ξαναδουλεύεις στοχευμένα</h3>
        <p>Γρήγορο τεστ μόνο πάνω στα λάθη σου, ή εξάσκηση ανά ενότητα.</p>
      </div>
      <div class="step">
        <span class="step-num">04</span>
        <h3>Το "μαθαίνεις" επίσημα</h3>
        <p>3 συνεχόμενες σωστές και βγαίνει — με επαλήθευση μερικά τεστ μετά.</p>
      </div>
    </div>
  </div>
</section>

<section class="block" id="features">
  <div class="wrap">
    <div class="section-head">
      <span class="eyebrow">Λειτουργίες</span>
      <h2>Ό,τι χρειάζεται μια σοβαρή προετοιμασία.</h2>
      <p>Χτισμένο γύρω από το πώς μαθαίνει πραγματικά κάποιος, όχι γύρω από ένα quiz template.</p>
    </div>
    <div class="feature-grid">
      <div class="feature">
        <div class="feature-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="4" y="3" width="16" height="18" rx="2"/><path d="M8 8h8M8 12h8M8 16h5"/></svg></div>
        <h3>Τεστ Προσομοίωσης</h3>
        <p>25 ερωτήσεις, 25 λεπτά, δομή πιστή στο πραγματικό διαγωνισμό ΑΣΕΠ.</p>
      </div>
      <div class="feature">
        <div class="feature-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 7h18M3 12h18M3 17h12"/></svg></div>
        <h3>Έξυπνο Μητρώο Λαθών</h3>
        <p>Ομαδοποιημένο ανά θεματική ενότητα, με μετρητή "φορές λάθος" ανά ερώτηση.</p>
      </div>
      <div class="feature">
        <div class="feature-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 3"/></svg></div>
        <h3>Κάλυψη Χωρίς Επανάληψη</h3>
        <p>Καμία ερώτηση δεν ξαναβλέπεις πριν εμφανιστεί όλη η ύλη τουλάχιστον μία φορά.</p>
      </div>
      <div class="feature">
        <div class="feature-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V9M12 19V5M20 19v-7"/></svg></div>
        <h3>Ιστορικό Επιδόσεων</h3>
        <p>Γράφημα προόδου, μέσος όρος, τάση — βλέπεις αν πραγματικά βελτιώνεσαι.</p>
      </div>
      <div class="feature">
        <div class="feature-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3l8 4v5c0 5-3.5 8-8 9-4.5-1-8-4-8-9V7l8-4z"/></svg></div>
        <h3>Auto-save Προόδου</h3>
        <p>Αν χαθεί η σύνδεση μέσα σε τεστ, συνεχίζεις ακριβώς από εκεί που έμεινες.</p>
      </div>
      <div class="feature">
        <div class="feature-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 20V10M18 20V4M6 20v-4"/></svg></div>
        <h3>Εξάσκηση ανά Ενότητα</h3>
        <p>Διάλεξε συγκεκριμένη ύλη και εξασκήσου χωρίς χρονόμετρο, όποτε θες.</p>
      </div>
    </div>
  </div>
</section>

<section class="block" id="pricing">
  <div class="wrap">
    <div class="section-head">
      <span class="eyebrow">Δωρεάν vs Premium</span>
      <h2>Ξεκίνα δωρεάν. Δες αν σου κάνει.</h2>
      <p>Καμία κάρτα δεν χρειάζεται για να ξεκινήσεις.</p>
    </div>
    <div class="pricing-grid">
      <div class="price-card">
        <span class="price-tag">Δωρεάν</span>
        <div class="price-amount">0€</div>
        <h3>Για να το δοκιμάσεις</h3>
        <p class="desc">Αρκετό για να καταλάβεις αν σου ταιριάζει η μέθοδος.</p>
        <ul class="price-list">
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>2 πλήρη Τεστ Προσομοίωσης</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Μητρώο Λαθών & Ιστορικό, χωρίς όριο</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>1 Εξάσκηση ανά ενότητα</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>1 Γρήγορο Τεστ Μητρώου</li>
        </ul>
        <a class="btn btn-ghost" href="https://app.asepstudy.gr" style="width:100%;justify-content:center">Δημιουργία Λογαριασμού</a>
      </div>
      <div class="price-card highlight">
        <span class="price-tag">Premium</span>
        <div class="price-amount">69€ <span style="font-size:1rem;font-weight:500;color:rgba(20,33,58,0.6)">εφάπαξ</span></div>
        <h3>Για ολόκληρη την προετοιμασία</h3>
        <p class="desc">Μία πληρωμή, καμία συνδρομή, πρόσβαση για πάντα.</p>
        <ul class="price-list">
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Απεριόριστα Τεστ Προσομοίωσης</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Απεριόριστη Εξάσκηση σε όλες τις ενότητες</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Απεριόριστα Γρήγορα Τεστ Μητρώου</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Όλα τα δωρεάν, χωρίς όρια</li>
        </ul>
        <a class="btn btn-primary" href="https://app.asepstudy.gr" style="width:100%;justify-content:center">Δήλωσε Ενδιαφέρον</a>
      </div>
    </div>
  </div>
</section>

<section class="block" id="faq">
  <div class="wrap">
    <div class="section-head">
      <span class="eyebrow">Συχνές Ερωτήσεις</span>
      <h2>Πριν ρωτήσεις, μάλλον απαντάμε εδώ.</h2>
    </div>
    <div class="faq-list">
      <details open>
        <summary>Χρειάζεται κάρτα για να ξεκινήσω;</summary>
        <p>Όχι. Η εγγραφή και τα 2 πρώτα τεστ είναι εντελώς δωρεάν, χωρίς κανένα στοιχείο πληρωμής.</p>
      </details>
      <details>
        <summary>Πώς ξέρω ότι "έμαθα" πραγματικά μια ερώτηση;</summary>
        <p>Χρειάζονται 3 συνεχόμενες σωστές απαντήσεις σε πραγματικά τεστ (όχι σε ελεύθερη εξάσκηση) για να βγει μια ερώτηση από το ενεργό μητρώο. Μετά από μερικά τεστ ακόμα, επανεμφανίζεται μία φορά για επαλήθευση.</p>
      </details>
      <details>
        <summary>Θα ξαναδώ την ίδια ερώτηση συνέχεια;</summary>
        <p>Όχι εκτός αν την έχεις κάνει λάθος. Οι "κανονικές" ερωτήσεις κάθε τεστ τραβιούνται με προτεραιότητα από όσες δεν έχεις ξαναδεί, μέχρι να καλυφθεί όλη η ύλη.</p>
      </details>
      <details>
        <summary>Τι γίνεται με το Premium αν δεν είμαι σίγουρος/η ακόμα;</summary>
        <p>Δήλωσε ενδιαφέρον χωρίς καμία δέσμευση — θα ενημερωθείς όταν είναι πλήρως διαθέσιμο για αγορά.</p>
      </details>
    </div>
  </div>
</section>

<section class="block">
  <div class="wrap">
    <div class="cta-band">
      <h2>Ξεκίνα το πρώτο σου τεστ σήμερα.</h2>
      <p>Δύο δωρεάν τεστ, καμία κάρτα, κανένας κίνδυνος — μόνο για να δεις αν σου ταιριάζει.</p>
      <a class="btn btn-primary btn-lg" href="https://app.asepstudy.gr">Ξεκίνα Δωρεάν Τώρα</a>
    </div>
  </div>
</section>

<footer>
  <div class="wrap footer-inner">
    <div class="footer-copy">© 2026 ASEP Study — asepstudy.gr</div>
    <div class="footer-links">
      <a href="https://app.asepstudy.gr">Σύνδεση</a>
      <a href="https://app.asepstudy.gr">Όροι & Απόρρητο</a>
    </div>
  </div>
</footer>

<script>
  // Ζωντανός μετρητής ερωτήσεων από το Supabase - αντικατέστησε τις 2 τιμές
  // παρακάτω με τα δικά σου (ανεξίγγελτο να είναι δημόσιο - το anon key
  // είναι σχεδιασμένο να είναι public, η ασφάλεια κρατιέται από RLS).
  const SUPABASE_URL = "https://enkypdsegnxwzvntazok.supabase.co";
  const SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVua3lwZHNlZ254d3p2bnRhem9rIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODQzNTQ4ODMsImV4cCI6MjA5OTkzMDg4M30.XoqVaF_TqDq9nUHAKrHnFKCn9gqXpG6qMyF0rlanR6c";

  function animateCount(el, target) {
    const start = 0;
    const duration = 1100;
    const startTime = performance.now();
    function tick(now) {
      const p = Math.min((now - startTime) / duration, 1);
      const eased = 1 - Math.pow(1 - p, 3);
      el.textContent = Math.round(start + (target - start) * eased).toLocaleString('el-GR');
      if (p < 1) requestAnimationFrame(tick);
    }
    requestAnimationFrame(tick);
  }

  async function loadStats() {
    const el = document.getElementById('stat-questions');
    try {
      const res = await fetch(`${SUPABASE_URL}/rest/v1/questions?select=id`, {
        method: 'HEAD',
        headers: {
          apikey: SUPABASE_ANON_KEY,
          Authorization: `Bearer ${SUPABASE_ANON_KEY}`,
          Prefer: 'count=exact',
        },
      });
      const range = res.headers.get('content-range');
      const total = range ? parseInt(range.split('/')[1], 10) : null;
      if (total && !Number.isNaN(total)) {
        animateCount(el, total);
      } else {
        el.textContent = '1.900+';
      }
    } catch (e) {
      el.textContent = '1.900+';
    }
  }

  document.addEventListener('DOMContentLoaded', loadStats);
</script>

</body>
</html>